services:
  powerguardian-connector:
    image: ghcr.io/powerguardianos/powerguardian-connector-docker:latest
    network_mode: host
    privileged: true
    volumes:
      - ./data:/data
      - /etc/nut:/etc/nut
      - /var/run/nut:/var/run/nut
    devices:
      - /dev/bus/usb:/dev/bus/usb
    environment:
      - PG_DATA_DIR=/data
      - PG_AGENT_KEY=
      - PG_WATCHTOWER_URL=http://localhost:9000
      - PG_WATCHTOWER_TOKEN=changeme
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
    restart: unless-stopped

  watchtower:
    image: containrrr/watchtower
    container_name: watchtower
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - WATCHTOWER_HTTP_API_TOKEN=changeme
      - WATCHTOWER_HTTP_API_UPDATE=true
      - WATCHTOWER_HTTP_API_PORT=9000
      - WATCHTOWER_LABEL_ENABLE=true
      - WATCHTOWER_CLEANUP=true
      - WATCHTOWER_NO_STARTUP_MESSAGE=true
